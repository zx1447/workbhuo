name: Auto-Wake DevTop (无头浏览器点击立即启动)

on:
  schedule:
    - cron: '*/14 * * * *'   # 每 14 分钟
  workflow_dispatch:

env:
  WEBTOP_URL: ${{ secrets.WEBTOP_URL }}
  WEBTOP_USER: ${{ secrets.WEBTOP_USER }}
  WEBTOP_PASSWORD: ${{ secrets.WEBTOP_PASSWORD }}
  # 裸地址(停了之后这页带"立即启动"按钮) 与 启动端点
  BARE_URL: https://webview.e2b.gz3.sandbox.cloudstudio.club/
  RESTART_URL: https://webview.e2b.gz3.sandbox.cloudstudio.club/?restart=true

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: 探测桌面 (仅 302 算真正在线)
        id: probe
        run: |
          code=$(curl -sS -m 90 -o /dev/null -w '%{http_code}' -u "$WEBTOP_USER:$WEBTOP_PASSWORD" "$WEBTOP_URL")
          echo "探测状态码: $code"
          if [ "$code" = "302" ]; then
            echo "online=true" >> "$GITHUB_OUTPUT"
            echo "✅ 沙箱在线 (302)"
          else
            echo "online=false" >> "$GITHUB_OUTPUT"
            echo "⚠️ 状态码 $code → 视为离线，准备无头浏览器唤醒"
          fi

      - name: 无头浏览器打开裸地址并点击「立即启动」
        if: steps.probe.outputs.online == 'false'
        timeout-minutes: 12
        run: |
          pip install --quiet playwright
          playwright install --with-deps chromium
          python3 - <<'PY'
          import os, time
          from playwright.sync_api import sync_playwright

          BARE   = os.environ['BARE_URL']
          url    = os.environ['WEBTOP_URL']
          user   = os.environ['WEBTOP_USER']
          pwd    = os.environ['WEBTOP_PASSWORD']
          restart= os.environ['RESTART_URL']

          with sync_playwright() as p:
              b   = p.chromium.launch()
              ctx = b.new_context(http_credentials={"username": user, "password": pwd})
              page = ctx.new_page()

              # 1) 打开裸地址(沙箱停了这页带"立即启动"按钮)
              print("[1] 打开裸地址:", BARE)
              page.goto(BARE, wait_until="load", timeout=60000)
              time.sleep(4)

              clicked = False
              selectors = [
                  "text=立即启动",
                  ".button.button-primary:has-text('立即启动')",
                  "button:has-text('立即启动')",
                  "#startBtn",
              ]
              for sel in selectors:
                  try:
                      btn = page.locator(sel)
                      if btn.count() > 0 and btn.first.is_visible(timeout=5000):
                          btn.first.click()
                          clicked = True
                          print(f"[2] ✅ 已点击「立即启动」(选择器: {sel})")
                          break
                  except Exception:
                      continue

              if not clicked:
                  print("[2] ⚠️ 未定位到按钮，直接访问 ?restart=true 端点")
                  try:
                      page.goto(restart, timeout=30000)
                      print(f"[2] 已访问: {restart}")
                  except Exception as e:
                      print(f"[2] restart 访问异常: {e}")

              print("[3] 等待沙箱启动...")
              time.sleep(30)
              b.close()
          PY

      - name: 唤醒后复探确认
        if: steps.probe.outputs.online == 'false'
        run: |
          sleep 10
          code=$(curl -sS -m 90 -o /dev/null -w '%{http_code}' -u "$WEBTOP_USER:$WEBTOP_PASSWORD" "$WEBTOP_URL")
          echo "唤醒后状态码: $code"
          if [ "$code" = "302" ]; then
            echo "✅ 沙箱已恢复在线"
          else
            echo "⚠️ 仍不可达 ($code)"
          fi
